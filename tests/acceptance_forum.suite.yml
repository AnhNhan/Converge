class_name: ForumWebGuy
modules:
    enabled:
      - PhpBrowser
      - ForumWebHelper
      - Db
    config:
        Db:
            dsn: 'mysql:host=127.0.0.1;dbname=converge_forum'
            user: 'converge'
            password: ''
            dump: tests/_data/db_forum.sql
        PhpBrowser:
            url: 'http://converge.dev/'
