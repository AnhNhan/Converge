class_name: TagTestGuy
modules:
    enabled:
      - Db
      - Doctrine2
      - TagTestHelper
    config:
        Db:
            dsn: 'mysql:host=127.0.0.1;dbname=converge_user'
            user: 'converge'
            password: ''
            cleanup: false
            populate: false
