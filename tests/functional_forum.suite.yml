class_name: ForumTestGuy
modules:
    enabled:
      - Db
      - Doctrine2
      - ForumTestHelper
    config:
        Db:
            dsn: 'mysql:host=127.0.0.1;dbname=converge_user'
            user: 'converge'
            password: ''
            cleanup: false
            populate: false
